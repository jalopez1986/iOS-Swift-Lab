# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CusoPlatzi' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CusoPlatzi
  pod 'PopupDialog', '~> 1.1'
  pod 'Alamofire', '~> 5.2'

  target 'CusoPlatziTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CusoPlatziUITests' do
    # Pods for testing
  end

end
